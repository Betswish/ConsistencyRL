name: CRL
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - pip=25.0
  - python=3.11
  - pip:
      - huggingface-hub
      - torch==2.7.1
      - scipy
      - tokenizers
      - tqdm
      - transformers>=4.55.0
      - vllm==0.10.1.1
      - numpy
      - python-dotenv==1.1.1
      - datasets
